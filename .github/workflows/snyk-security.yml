# Snyk security analysis (Open Source + Code) — mismo setup que mic-authservice,
# adaptado a un proyecto autónomo (sin GitHub Packages privados).
# Requiere el secret SNYK_TOKEN. Token gratuito en https://snyk.io/login

name: Snyk Security

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]

permissions:
  contents: read
  packages: read
  security-events: read

jobs:
  snyk:
    permissions:
      contents: read # for actions/checkout to fetch code
      security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
      actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
    runs-on: ubuntu-latest
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: maven
      - name: Set up Snyk CLI to check for security issues
        uses: snyk/actions/setup@806182742461562b67788a64410098c9d9b96adb
      - name: Build project with Maven
        run: mvn -B clean install -DskipTests

      - name: Snyk Code test
        run: snyk code test --sarif > snyk-code.sarif || true

      # Runs Snyk Open Source (SCA) analysis and uploads result to Snyk.
      - name: Snyk Open Source monitor
        run: snyk monitor --all-projects || true

      # Runs Snyk Infrastructure as Code (IaC) analysis only when IaC files exist.
      - name: No IaC files detected
        if: ${{ hashFiles('**/*.tf', '**/*.tf.json', '**/*.bicep', '**/cloudformation*.y*ml', '**/k8s/**/*.y*ml', '**/helm/**/*.y*ml') == '' }}
        run: echo "No valid IaC files found. Skipping Snyk IaC scan."

      - name: Snyk IaC test and report
        if: ${{ hashFiles('**/*.tf', '**/*.tf.json', '**/*.bicep', '**/cloudformation*.y*ml', '**/k8s/**/*.y*ml', '**/helm/**/*.y*ml') != '' }}
        run: snyk iac test --report || true

      # Push the Snyk Code results into GitHub Code Scanning tab
      - name: Upload result to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: snyk-code.sarif
          wait-for-processing: true
